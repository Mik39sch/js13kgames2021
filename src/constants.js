import { canvas } from './global'

export const PLAYER_HEIGHT = 97;
export const PLAYER_WIDTH = 72;
export const LOAD_PADDING = 3;

export const STAGE_WIDTH = canvas.width;
export const STAGE_HEIGHT = canvas.height;

export const Y_LOAD_WIDTH = PLAYER_HEIGHT + LOAD_PADDING;
export const X_LOAD_WIDTH = PLAYER_WIDTH + LOAD_PADDING;

export const STRING_COLOR = "gold";
